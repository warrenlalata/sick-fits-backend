# import * from './generated/prisma.graphql'

type Mutation {
    createItem(title: String, description: String, image: String, largeImage: String, price: Int): Item!
}

type Query {
    items: [Item]! # 'items' can be found inside src/resolvers/Query
}